<%= render "components/navbar" %>

<div class="main-container align-center mx-auto">
  <div class="flex justify-center mb-4 mt-20">
    <h1 class="text-3xl font-bold mb-6">Modifier le profile de l'artiste</h1>
  </div>

  <div class="form-container bg-white"><%= render 'form' %></div>
</div>
