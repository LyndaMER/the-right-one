<%= render "components/navbar" %>

<div class= m-8>
  <h1 class="text-4xl font-extrabold dark:text-black">
      Hello Anne-Sophie,
  </h1>
  <p class="text-2xl font-extrabold dark:text-black">
    Bienvenue sur The Right One ! 🎬</p>
</div>

<%# card des dernières auditions %>
<div class="max-w-sm rounded overflow-hidden shadow-lg flex">
  <div>
  <h2 class="mb-2 text-lg font-semibold text-gray-900 dark:text-black">Audition les plus récentes</h2>
  <ul class="max-w-md space-y-1 text-gray-500 list-none list-inside dark:text-black">
      <% @auditions.each do |audition| %>
    <li>
      <div class="flex items-center space-x-4 rtl:space-x-reverse">
        <div class="flex-1 min-w-0">
          <p class="text-sm font-medium text-gray-900 truncate dark:text-black">
            <%= audition.name%>
          </p>
        </div>
        <div class="inline-flex items-center text-base font-semibold text-gray-900 dark:text-black">
            <%= audition.date%>
        </div>
      </div>
    </li>
    <li>
      <div class="flex items-center space-x-4 rtl:space-x-reverse">
        <div class="flex-1 min-w-0">
          <p class="text-sm font-medium text-gray-900 truncate dark:text-black">
            <%= audition.name%>
          </p>
        </div>
        <div class="inline-flex items-center text-base font-semibold text-gray-900 dark:text-black">
            <%= audition.date%>
        </div>
      </div>
    </li>
    <li>
      <div class="flex items-center space-x-4 rtl:space-x-reverse">
        <div class="flex-1 min-w-0">
          <p class="text-sm font-medium text-gray-900 truncate dark:text-black">
            <%= audition.name%>
          </p>
        </div>
        <div class="inline-flex items-center text-base font-semibold text-gray-900 dark:text-black">
            <%= audition.date%>
        </div>
      </div>
    </li>
  </ul>
  <% end %>
  </div>
</div>
<button type="button" class="rounded-full bg-terracotta-600 px-3 py-1.5 text-sm font-semibold text-black shadow-sm hover:bg-terracotta-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-terracotta-600">+ Ajouter une audition</button>

<%# card des derniers artistes insccrits %>
<div class="max-w-sm rounded overflow-hidden shadow-lg flex">
<p><%= @users.count %></p>

<button type="button" class="rounded-full bg-white px-3 py-1.5 text-sm font-semibold text-terracotta-600 shadow-sm ring-1 ring-inset ring-terracotta-600 hover:bg-terracotta-50">+ Ajouter une audition</button>
</div>

<div id="default-carousel" class="relative w-full" data-carousel="slide">
  <div class="relative h-56 overflow-hidden rounded-lg md:h-96">
    <% @users.each do |user| %>
      <div class="hidden duration-700 ease-in-out " data-carousel-item>
          <img src="<%= user.pola_face %>" alt="Photo d'un artiste" class="absolute block w-full -translate-x-1/2 -translate-y-1/2 top-1/2 left-1/2">
          <%#= user.first_name %> <%#= user.last_name %>
          <%#= user.address%>
          <%#= user.birth_date%>

      </div>
    <% end %>
  </div>
  <div class="absolute z-30 flex -translate-x-1/2 bottom-5 left-1/2 space-x-3 rtl:space-x-reverse">
    <button type="button" class="w-3 h-3 rounded-full" aria-current="true" aria-label="Slide 1" data-carousel-slide-to="0"></button>
    <button type="button" class="w-3 h-3 rounded-full" aria-current="false" aria-label="Slide 2" data-carousel-slide-to="1"></button>
    <button type="button" class="w-3 h-3 rounded-full" aria-current="false" aria-label="Slide 3" data-carousel-slide-to="2"></button>
  </div>
  <button type="button" class="absolute top-0 start-0 z-30 flex items-center justify-center h-full px-4 cursor-pointer group focus:outline-none" data-carousel-prev>
      <span class="inline-flex items-center justify-center w-10 h-10 rounded-full bg-white/30 dark:bg-gray-800/30 group-hover:bg-white/50 dark:group-hover:bg-gray-800/60 group-focus:ring-4 group-focus:ring-white dark:group-focus:ring-gray-800/70 group-focus:outline-none">
          <svg class="w-4 h-4 text-white dark:text-gray-800 rtl:rotate-180" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 6 10">
              <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 1 1 5l4 4"/>
          </svg>
          <span class="sr-only">Previous</span>
      </span>
  </button>
  <button type="button" class="absolute top-0 end-0 z-30 flex items-center justify-center h-full px-4 cursor-pointer group focus:outline-none" data-carousel-next>
      <span class="inline-flex items-center justify-center w-10 h-10 rounded-full bg-white/30 dark:bg-gray-800/30 group-hover:bg-white/50 dark:group-hover:bg-gray-800/60 group-focus:ring-4 group-focus:ring-white dark:group-focus:ring-gray-800/70 group-focus:outline-none">
          <svg class="w-4 h-4 text-white dark:text-gray-800 rtl:rotate-180" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 6 10">
              <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 9 4-4-4-4"/>
          </svg>
          <span class="sr-only">Next</span>
      </span>
  </button>
</div>

<%= render "components/footer"%>
